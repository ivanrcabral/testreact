/*
* Retorna el formato de tiempo requerido
* millisecondsDate Number
* @returns String
*/
export function getFormattedDate(millisecondsDate) {
  let formattedDate = 'Para Crear funcion'

  // Ejemplo de retorno '25/01/20'
  return formattedDate;
}
